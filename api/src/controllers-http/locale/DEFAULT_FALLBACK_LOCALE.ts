/**
 * Default locale used for the app if no locale is explicitly specified.
 */
export const DEFAULT_FALLBACK_LOCALE = "en";

/**
 * Default locales used for the app if no locales is explicitly specified.
 */
export const DEFAULT_FALLBACK_LOCALES = [DEFAULT_FALLBACK_LOCALE];
