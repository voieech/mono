# dal (Data Access Layer)
- Using a simplified data repository pattern, using TS const objects instead of classes to keep things simple.
- Users can still just use apiDB directly, but this can start as a base for the more common queries first