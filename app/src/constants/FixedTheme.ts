/**
 * For now, use this as a flag to temporarily fix Theme to be always light mode
 * until dark mode is supported in the future.
 */
export const isThemeFixed = true;

/**
 * For now, temporarily only support dark mode.
 */
export const DefaultTheme = "dark" as const;
